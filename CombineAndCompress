#!/bin/sh

yes | ffmpeg -i "$1" -i "$2" -map 0:v -map 1:a -vf scale=1280x720:flags=fast_bilinear -vcodec libx264 -tune zerolatency -preset ultrafast -movflags frag_keyframe+empty_moov+default_base_moof+faststart -crf "$4" -shortest -f mp4 "$3"